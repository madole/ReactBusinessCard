Demo at http://madole.github.io/ReactBusinessCard/ 

### Challenge React Vs React JSX:

- To build a business card using react
- Must read from a JSON config object
- Must use JSX
- Must be as modular as possible
